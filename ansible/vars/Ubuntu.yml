desktop_removable_files:
  - .config/xfce4/xfconf/xfce-perchannel-xml/xfce4-keyboard-shortcuts.xml

base_distro:
  - speed-test
  # vtop is not available in apt neither snap package managers (try using npm instead)
  # - vtop

browsers:
  - google-chrome-stable

network_distro:
  - openssh-server
