" to avoid conflicts with Tim Pope's fugitive and remote files:
let g:EditorConfig_exclude_patterns = ['fugitive://.*', 'scp://.*']

let g:EditorConfig_exec_path = "/home/pantoja/.asdf/shims/editorconfig"
